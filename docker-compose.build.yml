# Use this to build the image locally instead of pulling from ghcr.io
#   docker compose -f docker-compose.build.yml up -d
services:
  demobot:
    build: .
    container_name: mavv-demobot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
